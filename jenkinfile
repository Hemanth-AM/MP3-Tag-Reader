pipeline {
    agent any

    options {
        timeout(time: 2, unit: 'MINUTES')
        timestamps()
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }
    }
}
